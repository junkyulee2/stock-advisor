[browser]
gatherUsageStats = false

[theme]
base = "dark"
primaryColor = "#22c55e"
backgroundColor = "#0f172a"
secondaryBackgroundColor = "#1e293b"
textColor = "#f0f5ff"
font = "sans serif"

[server]
# Must be true on Streamlit Cloud to skip the interactive welcome prompt.
headless = true
